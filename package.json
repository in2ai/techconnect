{
  "name": "schemas-script",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "export:postgres": "drizzle-kit export --dialect=postgresql --schema=./schema/postgres.ts",
    "export:mysql": "drizzle-kit export --dialect=mysql --schema=./schema/mysql.ts",
    "export:sqlite": "drizzle-kit export --dialect=sqlite --schema=./schema/sqlite.ts"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.31.8"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
